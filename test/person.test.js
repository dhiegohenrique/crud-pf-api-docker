const expect = require('chai').expect
const HttpStatus = require('http-status-codes')

describe('Person', () => {
  beforeEach(() => {
  })

  describe('Should add new person', () => {
    it('Teste de exemplo', () => {
      expect(2).to.be.equal(2)
    })
  })
})
